define([], function() {
	//Attach substructures to module here
	var parser = {};
	
	parser.load = function(file) {
		//Load file for parsing
	};

	parser.parse = function(file_obj) {
		//Parse loaded file
	};

	return parser; //Return module
});